class Player{
    constructor(playerX, playerY){
        this.playerX = playerX;
        this.playerY = playerY;
    }

    isAlive(){
        //check if player is alive and return bool;
        if (true){
            console.log("player is alive");
        }
    }
}
